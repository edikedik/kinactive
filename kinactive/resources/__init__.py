"""
This module contains persistent resources downloaded alongside the Python code.

It contains the PK domain HMM profile and the models: ``DFG_classifier`` and
``kinactive_classifier``. The functions :func:`kinactive.io.load_dfg` and
:func:`kinactive.io.load_kinactive` are used to load these into the Python
interpreter.
"""